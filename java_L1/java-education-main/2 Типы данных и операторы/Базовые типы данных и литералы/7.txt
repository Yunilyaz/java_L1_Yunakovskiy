int a = 120;
byte b = a + 10;           // Ошибка компиляции, так как результат выражения int
byte c = (byte)(a + 10);   // c = 126 (переполнение)
byte d = a + 1;            // Ошибка компиляции, так как a + 1 возвращает int

Произойдет ошибка компиляции

